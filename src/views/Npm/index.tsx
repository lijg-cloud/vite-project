import React, { useState, useEffect, useReducer, useCallback, useContext, useRef, useMemo } from 'react'

const Npm: React.FC = ({ }) => {
  const [] = useState()

  useEffect(() => {

  }, [])

  return (
    <div>
      <h1>Npm</h1>
    </div>
  )
}
export default Npm